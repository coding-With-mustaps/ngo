import styled from "styled-components";
import { Flower } from "../../Styled-components/Button.style";

const AchievementContainer = styled.div`

`

const AchievedFlower = styled(Flower)``

export { AchievedFlower }