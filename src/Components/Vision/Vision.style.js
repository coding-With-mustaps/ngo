import styled from "styled-components";
import { Flower } from "../../Styled-components/Button.style";

const VisionContainer = styled.div`

`

const VisionFlower = styled(Flower)`
    left: 10px;
`

export { VisionContainer, VisionFlower };